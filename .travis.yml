language: python
python:
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"
install:
    - pip install .
    - pip install --default-timeout 60 -r requirements.txt
script:
    - flake8 rdftools
    - pytest --cov=rdftools test/*.py
after_success:
    - coveralls
branches:
  only:
    - master